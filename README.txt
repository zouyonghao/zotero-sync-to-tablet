A Zotero plugin for sending and retrieving files from a directory.

After setting a target directory in the settings, it supports copying the specified file to the target directory and retrieving the file from the target directory (overwrite).

This plugin does not have other features, like extracting annotations.
